# The official Qwen runtime currently pins its compatible Transformers release.
qwen-asr==0.0.6

# Qwen imports PyTorch directly but does not declare it as a package dependency.
# This release has native Apple Silicon wheels and supports MPS acceleration.
torch==2.8.0

# Pin the cache/download API used for explicit first-run model provisioning.
huggingface-hub==0.36.0

# Native MLX execution on Apple Silicon avoids slow PyTorch/MPS fallback paths.
mlx-qwen3-asr==0.3.5; platform_system == "Darwin" and platform_machine == "arm64"
